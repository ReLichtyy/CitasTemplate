{
  "$schema": "https://json.schemastore.org/nest-cli",
  "collection": "@nestjs/schematics",
  "sourceRoot": "src",
  "compilerOptions": {
    "deleteOutDir": true,
    "//": "Sin esto la imagen de la confirmacion no llega a dist/ y el envio falla solo en produccion, que es el peor sitio para enterarse.",
    "assets": [{ "include": "notificaciones/assets/**", "outDir": "dist" }],
    "watchAssets": true
  }
}
